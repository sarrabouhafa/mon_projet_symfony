<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Détails Auteur</title>
</head>
<body>
    <h1>Détails de l’auteur</h1>

    <img src="{{ author.picture }}" alt="{{ author.username }}" width="200">
    <p><strong>Nom :</strong> {{ author.username }}</p>
    <p><strong>Email :</strong> {{ author.email }}</p>
    <p><strong>Nombre de livres :</strong> {{ author.nb_books }}</p>

    <a href="{{ path('app_authors') }}">⬅ Retour à la liste</a>
</body>
</html>
