<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des auteurs</title>
</head>
<body>
    <h1>Liste des auteurs</h1>

    {# 4. Vérification si tableau vide ou non défini #}
    {% if authors is defined and authors|length > 0 %}

        <ul>
            {% for author in authors %}
                <li>
                    <img src="{{ author.picture }}" alt="{{ author.username }}" width="100">
                    <strong>{{ author.username|upper }}</strong>  {# 5. Nom en majuscule #}
                    <br>Email : {{ author.email }}
                    <br>Nombre de livres : {{ author.nb_books }}
                    <br><a href="{{ path('app_author_details', {id: author.id}) }}">Détails</a> {# 8. Lien détails #}
                </li>
                <hr>
            {% endfor %}
        </ul>

        {# 6. Nombre des auteurs #}
        <p><strong>Nombre d’auteurs :</strong> {{ authors|length }}</p>

        {# 7. Nombre total de livres #}
        <p><strong>Total des livres :</strong> {{ authors|map(a => a.nb_books)|reduce((carry, n) => carry + n) }}</p>

    {% else %}
        <p>Aucun auteur trouvé.</p>
    {% endif %}
</body>
</html>
